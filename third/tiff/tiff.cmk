#
# Xin YUAN, 2019, BSD (2)
#

# The path of tiff library

MACRO(SET_TIFF_PATH strRoot)
	INCLUDE_DIRECTORIES("${strRoot}/third/tiff/tiff-4.0.10/libtiff"
						)
ENDMACRO(SET_TIFF_PATH)

SET(STRING_TIFF_TARGET "tiff")

# end
